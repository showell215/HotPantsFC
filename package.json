{
  "name": "Hot_Pants_FC",
  "version": "0.0.1",
  "description": "Hot Pants FC",
  "engines": {
    "node": "4.2"
  },
  "scripts": {
    "watch": "sass --watch src/styles:dist",
    "prewatch": "npm run css",
    "css": "sass src/styles/main.scss dist/main.css",
    "precss": "npm run lint",
    "lint": "scss-lint && eslint app.js src/js/**/*.js"
  },
  "devDependencies": {
    "access-log": "^0.3.9",
    "autoprefixer": "^6.7.3",
    "eslint": "^3.17.1",
    "scss-lint": "^0"
  }
}
