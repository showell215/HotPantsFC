{
  "name": "Hot_Pants_FC",
  "version": "0.0.1",
  "description": "Hot Pants FC",
  "engines": {
    "node": "4.2"
  },
  "scripts": {
    "watch": "sass --watch src/styles:dist",
    "prewatch": "sass src/styles/main.scss dist/main.css",
    "lint": "scss-lint src/styles/*"
  },
  "devDependencies": {
    "autoprefixer": "^6.7.3"
  }
}
